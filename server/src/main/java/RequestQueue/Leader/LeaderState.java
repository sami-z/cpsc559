package RequestQueue.Leader;

/**
 * Class used to store the global state of the current request queue
 *
 * State include the leaderIP of the leader request queue, and the current IP of the request queue
 * */
public class LeaderState {
    public static String leaderIP;
    public static String serverIP;
}
